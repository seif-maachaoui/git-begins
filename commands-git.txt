Pour cloner un repository distant :
git clone (ici placer le lien d'un dépot existant)

Les 3 commandes de push :
git remote add origin (ici placer le lien d'un dépot existant)
git branch -M main
git push -u origin main
